{
  "name": "def",
  "version": "1.0.0",
  "description": "",
  "author": "Deniz Matar",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/denizmatar/def.git"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "npm run compile",
    "test": "npm test",
    "postinstall": "(cd packages/contracts && yarn install); (cd packages/wallet && yarn install)"
  }
}
